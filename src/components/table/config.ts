import {TObj} from '../../core/store/types'

export const defaultStyles: TObj = {
  textAlign: 'left',
}

export const keyboardKeys = [
  'Enter',
  'Tab',
  'ArrowLeft',
  'ArrowRight',
  'ArrowDown',
  'ArrowUp',
]

export const FIRST_CELL_ID = '[data-id="1:A"]'

export const INITIAL_ROW_COUNT = 15
